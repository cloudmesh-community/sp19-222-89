#things that go here include: name, email, readme, very minimum definitions for file structure
from setuptools import setup
setup(name='prime', version='1.0.0', url='', author='Brandon Fischer', packages=[''])
 
